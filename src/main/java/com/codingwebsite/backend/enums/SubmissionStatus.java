package com.codingwebsite.backend.enums;

/**
 * Submission status after code execution
 */
public enum SubmissionStatus {
    PENDING,
    ACCEPTED,
    WRONG_ANSWER,
    RUNTIME_ERROR,
    TIME_LIMIT_EXCEEDED,
    COMPILATION_ERROR
}
